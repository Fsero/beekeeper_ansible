{
	"fqdn": "{{ansible_hostname}}",
    {% for ip in hostvars[ansible_hostname]['ansible_all_ipv4_addresses'] | ipaddr(firewall_ipaddr_filter) %}
    "ipv4": "{{ip}}",
    {% endfor %}
    "provider": "{{hostvars[ansible_hostname]['provider']}}}",
    "enabled": true
}